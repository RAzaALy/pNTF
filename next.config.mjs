/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["www.shutterstock.com","images.unsplash.com","via.placeholder.com"],
      },
};



export default nextConfig;
